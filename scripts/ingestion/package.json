{
  "name": "@vemetric/ingestion-scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "compare": "bun --env-file=../../.env compare/compare-refs.ts",
    "loadtest": "bun --env-file=../../.env loadtest/run.ts",
    "lint": "eslint . --max-warnings 0",
    "tsc": "tsc --noEmit",
    "prod-check": "bun prod-check/check.ts",
    "benchmark": "bun --env-file=../../.env benchmark/queries.ts",
    "rehearsal": "bun rehearsal/run.ts"
  },
  "dependencies": {
    "bullmq": "^5.73.1",
    "ioredis": "^5.10.1"
  },
  "devDependencies": {
    "@types/bun": "1.3.11",
    "@vemetric/eslint-config": "workspace:*",
    "eslint": "^9.18.0",
    "typescript": "^5.7.3"
  }
}
