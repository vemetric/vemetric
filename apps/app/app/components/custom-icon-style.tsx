import type { BoxProps } from '@chakra-ui/react';
import { Box } from '@chakra-ui/react';

export const CustomIconStyle = (props: BoxProps) => <Box filter="grayscale(0.15)" opacity={0.9} {...props} />;
