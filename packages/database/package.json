{
  "name": "database",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "db:generate": "dotenv -e ../../.env -- prisma generate",
    "db:migrate": "dotenv -e ../../.env -- prisma migrate dev --create-only",
    "db:deploy": "dotenv -e ../../.env -- prisma migrate deploy",
    "lint": "eslint . --max-warnings 0"
  },
  "dependencies": {
    "@prisma/client": "^5.4.2",
    "@vemetric/common": "workspace:*",
    "nanoid": "^5.0.4"
  },
  "devDependencies": {
    "@vemetric/eslint-config": "workspace:*",
    "dotenv-cli": "^10.0.0",
    "prisma": "^5.4.2"
  }
}
