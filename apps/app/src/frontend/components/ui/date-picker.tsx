import { DatePicker } from '@ark-ui/react/date-picker';
import { chakra } from '@chakra-ui/react';

export const DatePickerContent = chakra(DatePicker.Content);
export const DatePickerTable = chakra(DatePicker.Table);
export const DatePickerTableHeader = chakra(DatePicker.TableHeader);
export const DatePickerTableRow = chakra(DatePicker.TableRow);
export const DatePickerTableCell = chakra(DatePicker.TableCell);
export const DatePickerTableCellTrigger = chakra(DatePicker.TableCellTrigger);
